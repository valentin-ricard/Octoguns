mod systems {
    mod start; 
    mod spawn;
    mod actions;
}

mod models {
    mod bullet;
    mod sessions;
    mod map;
    mod character;
}

mod lib {
    mod defaultSpawns;
    mod helpers;
    mod simulate;
}

mod consts;
mod types;
