mod systems {
    mod start; 
    mod spawn;
    mod actions;
}

mod models {
    mod bullet;
    mod sessions;
    mod map;
    mod character;
    mod global;
    mod player;
}

mod lib {
    mod defaultSpawns;
    mod helpers;
    mod simulate;
    mod shoot;
}

mod tests {
    mod helpers;
   // mod test_world;
}

mod consts;
mod types;
