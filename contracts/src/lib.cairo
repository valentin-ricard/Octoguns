mod systems {
    mod start; 
    mod spawn;
    mod move;
}

mod models {
    mod bullet;
    mod sessions;
    mod map;
    mod character;
}

mod lib {
    mod defaultSpawns;
    mod data_mover;
    mod simulate;
}

mod consts;
mod types;